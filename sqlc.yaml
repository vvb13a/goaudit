version: "2"
sql:
  - engine: "sqlite"
    schema: "sql/migrations"
    queries: "sql/queries"
    gen:
      go:
        package: "store"
        out: "store"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true